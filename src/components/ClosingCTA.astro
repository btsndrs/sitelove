---
interface Props {
  headline: string;
  description: string;
  ctaLabel: string;
  ctaHref: string;
  contactLine: string;
  phoneLabel: string;
  privacyLabel: string;
  privacyHref: string;
}
const { headline, description, ctaLabel, ctaHref, contactLine, phoneLabel, privacyLabel, privacyHref } = Astro.props;
---

<section class="bg-brand py-20 md:py-28 relative overflow-hidden">
  <!-- decorative accent -->
  <div class="absolute bottom-0 right-0 w-64 h-64 bg-brand-accent/5 translate-x-1/2 translate-y-1/2 rounded-full blur-3xl pointer-events-none"></div>

  <div class="max-w-7xl mx-auto px-6 md:px-10 relative z-10">
    <div class="max-w-2xl">
      <h2 class="text-3xl md:text-5xl font-heading font-bold text-brand-light mb-6 leading-tight">{headline}</h2>
      <p class="text-brand-mid/60 leading-relaxed mb-10 text-lg">{description}</p>
      <a href={ctaHref} class="btn-primary">{ctaLabel}</a>
      <div class="mt-10 text-brand-mid/50 text-sm leading-relaxed">
        <p class="mb-2">{contactLine}</p>
        <p>email: <a href="mailto:office@qvarta.eu" class="hover:text-brand-accent transition-colors">office@qvarta.eu</a></p>
        <p>{phoneLabel}: <a href="tel:+36304446003" class="hover:text-brand-accent transition-colors">+36 30 444 6003</a></p>
      </div>
    </div>
  </div>
</section>

<footer class="bg-brand border-t border-white/5 py-8 px-6 md:px-10">
  <div class="max-w-7xl mx-auto flex flex-col md:flex-row justify-between items-center gap-4">
    <img 
      src="https://cdn.prod.website-files.com/6267babc3063512be3e7eb0d/6267bd9732d7d7a2c31bbe11_Qvarta%20logo.svg" 
      alt="Qvarta" 
      class="h-6 w-auto opacity-50"
    />
    <p class="text-brand-mid/30 text-xs">© {new Date().getFullYear()} Qvarta. All rights reserved.</p>
    <a href={privacyHref} class="text-brand-mid/30 text-xs hover:text-brand-accent transition-colors">{privacyLabel}</a>
  </div>
</footer>
